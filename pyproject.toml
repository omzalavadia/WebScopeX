[project]
name = "webscopex"
version = "3.0.0"
description = "Windows-first authorized web enumeration workspace"
requires-python = ">=3.11"
license = {text = "MIT"}

[tool.pytest.ini_options]
testpaths = ["tests"]
